I changed the error messages in postsController from "post not found" into "post with id 'the id used' was not found this helps imporve clarity, helps a user see which request failed and helps mak debugging an issue easier. i also changed "title is required to be a string" into "title is required and must be a non empty string" this helps reduce possible confusion about valid values and helps show users what is required of them.

in categoriescontroller nested conditionals where reduced by using early returns and centralized helper functions which flattened the controll flow and made it easier to read, instead of checking conditions inside multiple if blocks now itll immediately throw errors or return when something is invalid. I changed duplicated logic such as category lookup, input, validation, duplicate name checking, and error creation into reusable helper functions. this helped make the structure cleaner and made it easier to test while also providing imporved consistency. 