1: In larger applications raw SQL creates several issues shuch as making queries hard to manage, changes in schema become difficult to update everywhere and it also makes it harder to read.

2: an ORM lets you interact with a database using code instead of SQL 

3: AN orm replaces raw sql with method calls, it simplifies crud operations and helps reduce boilerplate code. 

4: you wouldnt want to use an ORM with complex queries, when full controll of sql is needed, and when working with a large scale and data heavy system 