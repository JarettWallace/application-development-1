1: Authentication is the process of verifying who a user is authorization is determining what a user is allowed to do after they are logged in.

2: It returns 403 because the user is already logged in but does not have permission 401 is only used when the user is not logged in.

3: Ownership checking ensures that users can only access their own data without it users could access or modify other users information 

4: Role based access controls what users can do based on their role, Ownership based access controls what users can access based on what they own 

5: Authorization must happen on the server because the client can be manipulated if checks are only on the client side users could bypass them and gain unauthorized access.